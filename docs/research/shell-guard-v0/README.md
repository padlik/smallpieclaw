# Shell Guard v0 Research Archive

This directory preserves the exploratory Shell Guard proposal work that was intentionally removed from active OpenSpec changes.

Status:

- Not an active OpenSpec change.
- Not apply-ready.
- Not an accepted/in-force ADR.
- Preserved as research/reference for future smaller proposals.

Contents:

- `openspec-change/` — parked OpenSpec artifacts from `guard-shell-tool-execution`.
- `adr-0005-draft-use-shell-guard-for-interactive-shell-tool-decisions.md` — parked draft ADR moved out of the repository-level `adr/` directory so it is not treated as in force.

If this work is revived, create one or more new smaller OpenSpec changes rather than applying this archived proposal directly.
