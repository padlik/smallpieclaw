python-telegram-bot>=21.0
httpx~=0.27
requests>=2.31
tomli==2.0.1
schedule==1.2.1
croniter>=1.4
ollama>=0.4.0
# Optional: graph-based memory (install to enable [graph_memory] enabled = true)
# ladybug>=0.7.0
# PTY shell backend — enables line-buffered, TTY-aware shell execution for
# sophisticated scripts. Already transitively present via python-telegram-bot.
ptyprocess>=0.7.0
